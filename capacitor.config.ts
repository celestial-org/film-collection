import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionic.filmcollection',
  appName: 'Film Collection',
  webDir: 'dist'
};

export default config;
